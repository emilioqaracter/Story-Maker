__pycache__/
*.pyc
.pytest_cache/
node_modules/
.env
.claude/settings.local.json

# El tablero. Los dos son generados: dist/ es el HTML de una pieza que sale de
# `npm run build`, y volcado.json es books/ volcado a JSON para el modo
# estatico. Se regeneran solos antes de cada arranque y de cada build.
ui/web/dist/
ui/web/src/datos/volcado.json
